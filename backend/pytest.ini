[pytest]
# Without this, a bare `pytest` run from backend/ also recurses into
# backend/repositories/<uuid>/ -- every cloned repo's own test suite would get
# collected too (and collide with each other: multiple repos can each have a
# tests/test_basic.py, which pytest can't tell apart without __init__.py files).
testpaths = tests
